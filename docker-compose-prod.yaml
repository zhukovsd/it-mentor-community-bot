services:
  bot:
    image: zhukovsd/it-mentor-community-bot:main
    env_file: .env.prod
    restart: always
    ports:
      - "${METRICS_PORT}:${METRICS_PORT}"
    depends_on:
      - database

  database:
    image: postgres
    env_file: .env.prod
    restart: always